{
  "name": "telegram-wax-signup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start-dev": "NODE_ENV=development nodemon --ignore '**/*.json' --exec node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "eosjs": "^20.0.3",
    "eosjs-ecc": "^4.0.7",
    "node-fetch": "^2.6.0",
    "telebot": "^1.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
